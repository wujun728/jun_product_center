<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.workfile.mapper.WorkflowAttachmentMapper">
    
    <resultMap type="WorkflowAttachment" id="WorkflowAttachmentResult">
        <result property="id"    column="id"    />
        <result property="businessId"    column="business_id"    />
        <result property="fileId"    column="file_id"    />
        <result property="delFlag"    column="del_flag"    />
        <result property="createId"    column="create_id"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateId"    column="update_id"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectWorkflowAttachmentVo">
        select id, business_id, file_id, del_flag, create_id, create_time, update_id, update_time from t_workflow_attachment
    </sql>

    <select id="selectWorkflowAttachmentList" parameterType="WorkflowAttachment" resultMap="WorkflowAttachmentResult">
        <include refid="selectWorkflowAttachmentVo"/>
        <where>  
            <if test="businessId != null  and businessId != ''"> and business_id = #{businessId}</if>
            <if test="fileId != null  and fileId != ''"> and file_id = #{fileId}</if>
            <if test="createId != null "> and create_id = #{createId}</if>
            <if test="updateId != null "> and update_id = #{updateId}</if>
        </where>
    </select>
    
    <select id="selectWorkflowAttachmentById" parameterType="String" resultMap="WorkflowAttachmentResult">
        <include refid="selectWorkflowAttachmentVo"/>
        where id = #{id}
    </select>

    <select id="selectWorkflowAttachmentByBusinessId" resultMap="WorkflowAttachmentResult">
        <include refid="selectWorkflowAttachmentVo"/>
        where del_flag = '0' and business_id = #{businessId}
    </select>

    <insert id="insertWorkflowAttachment" parameterType="WorkflowAttachment">
        insert into t_workflow_attachment
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null and id != ''">id,</if>
            <if test="businessId != null and businessId != ''">business_id,</if>
            <if test="fileId != null and fileId != ''">file_id,</if>
            <if test="delFlag != null">del_flag,</if>
            <if test="createId != null">create_id,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateId != null">update_id,</if>
            <if test="updateTime != null">update_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null and id != ''">#{id},</if>
            <if test="businessId != null and businessId != ''">#{businessId},</if>
            <if test="fileId != null and fileId != ''">#{fileId},</if>
            <if test="delFlag != null">#{delFlag},</if>
            <if test="createId != null">#{createId},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateId != null">#{updateId},</if>
            <if test="updateTime != null">#{updateTime},</if>
         </trim>
    </insert>

    <update id="updateWorkflowAttachment" parameterType="WorkflowAttachment">
        update t_workflow_attachment
        <trim prefix="SET" suffixOverrides=",">
            <if test="businessId != null and businessId != ''">business_id = #{businessId},</if>
            <if test="fileId != null and fileId != ''">file_id = #{fileId},</if>
            <if test="delFlag != null">del_flag = #{delFlag},</if>
            <if test="createId != null">create_id = #{createId},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateId != null">update_id = #{updateId},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where id = #{id}
    </update>

    <update id="remove" parameterType="WorkflowAttachment">
        update t_workflow_attachment
        <trim prefix="SET" suffixOverrides=",">
            <if test="delFlag != null">del_flag = #{delFlag},</if>
            <if test="updateId != null">update_id = #{updateId},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where file_id = #{fileId}
    </update>

    <delete id="deleteWorkflowAttachmentById" parameterType="String">
        delete from t_workflow_attachment where id = #{id}
    </delete>

    <delete id="deleteWorkflowAttachmentByIds" parameterType="String">
        delete from t_workflow_attachment where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
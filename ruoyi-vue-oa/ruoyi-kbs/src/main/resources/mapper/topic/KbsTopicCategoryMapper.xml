<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.kbs.mapper.KbsTopicCategoryMapper">

    <resultMap type="KbsTopicCategory" id="KbsTopicCategoryResult">
        <result property="id" column="id"/>
        <result property="sort" column="sort"/>
        <result property="name" column="name"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createId" column="create_id"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateId" column="update_id"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="selectKbsTopicCategoryVo">
        select id,
               sort,
               name,
               del_flag,
               create_id,
               create_by,
               create_time,
               update_id,
               update_by,
               update_time
        from t_kbs_topic_category
    </sql>

    <select id="selectKbsTopicCategoryList" parameterType="KbsTopicCategory" resultMap="KbsTopicCategoryResult">
        <include refid="selectKbsTopicCategoryVo"/>
        <where>
            <if test="name != null  and name != ''">and name like concat('%', #{name}, '%')</if>
            <if test="createId != null ">and create_id = #{createId}</if>
        </where>
    </select>

    <select id="selectKbsTopicCategoryById" parameterType="String" resultMap="KbsTopicCategoryResult">
        <include refid="selectKbsTopicCategoryVo"/>
        where id = #{id}
    </select>

    <insert id="insertKbsTopicCategory" parameterType="KbsTopicCategory">
        insert into t_kbs_topic_category
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null and id != ''">id,</if>
            <if test="sort != null">sort,</if>
            <if test="name != null and name != ''">name,</if>
            <if test="delFlag != null">del_flag,</if>
            <if test="createId != null">create_id,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateId != null">update_id,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null and id != ''">#{id},</if>
            <if test="sort != null">#{sort},</if>
            <if test="name != null and name != ''">#{name},</if>
            <if test="delFlag != null">#{delFlag},</if>
            <if test="createId != null">#{createId},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateId != null">#{updateId},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
        </trim>
    </insert>

    <update id="updateKbsTopicCategory" parameterType="KbsTopicCategory">
        update t_kbs_topic_category
        <trim prefix="SET" suffixOverrides=",">
            <if test="sort != null">sort = #{sort},</if>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="delFlag != null">del_flag = #{delFlag},</if>
            <if test="createId != null">create_id = #{createId},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateId != null">update_id = #{updateId},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteKbsTopicCategoryById" parameterType="String">
        delete
        from t_kbs_topic_category
        where id = #{id}
    </delete>

    <delete id="deleteKbsTopicCategoryByIds" parameterType="String">
        delete from t_kbs_topic_category where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="selectKbsTopicCategoryByIds" parameterType="String" resultMap="KbsTopicCategoryResult">
        <include refid="selectKbsTopicCategoryVo"/>
        where id in
        <foreach item="id" collection="list" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <update id="batchUpdate">
        update t_kbs_topic_category
        <trim prefix="SET" suffixOverrides=",">
            <if test="delFlag != null">del_flag = #{delFlag},</if>
            <if test="updateId != null">update_id = #{updateId},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where del_flag = '0' and id in
        <foreach collection="ids" item="item" index="index" open="" close="" separator=";">
            #{item}
        </foreach>
    </update>

    <select id="selectCategorySelectList" parameterType="String" resultType="KbsTopicCategoryOption">
        select id categoryId,
               name categoryName
        from t_kbs_topic_category
        where del_flag = '0'
          and create_id = #{createId}
        order by sort, create_time
    </select>
</mapper>